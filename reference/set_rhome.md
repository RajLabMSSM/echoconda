# Set `R_HOME`

Set the location of your R library when it is being called through a
python script (e.g. via the python package rpy2).

## Usage

``` r
set_rhome(conda_env = "echoR_mini", verbose = TRUE)
```

## Arguments

- conda_env:

  Conda environment to use.

- verbose:

  Print messages.
